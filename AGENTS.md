## Response style

- Use the installed `caveman` skill at `~/.codex/skills/caveman/SKILL.md` for every user request while working in this project. The user does not need to invoke `$caveman`.
- Use `full` intensity by default. Keep it active until the user says `stop caveman`, `normal mode`, or `/caveman off`.
